# 经典 23 种设计模式详解（评分器参考）

## 定位

设计模式不是并列的 23 个知识点，而是**四大要素 + 七大原则在常见场景下的标准解法**。评分时模式的用途是双向的：

1. **缺模式 → 扣分**：手写 switch 分派（该用状态/策略模式）、全局单例纠缠（该用入口组装/中介者）、巨型条件分支复述类型（该用多态）——按对应维度记违规，修复方向直接指名模式。
2. **模式病 → 同样扣分**：只有一种实现却套抽象工厂、无状态变化却套状态模式、为幻想中的扩展点加解释器/访问者——按"简单"属性受损与过度设计（维度 9）记违规。**模式是缺失时的解药，不是默认配料。**

评分报告的修复方向可以直接用模式名作词汇（"此处收敛为策略模式 + 配置选择"）。

## 设计模式最佳实践

1. **先有问题，再用模式**：模式解决的是重复出现的具体设计问题。套用前必须能一句话说出"这个模式解决我的什么问题"——说不出就是为模式而模式，直接按过度设计处理。
2. **重构到模式，而不是预先设计**：第一次用最简单的直写；同样的复杂度出现第三次，再收敛成模式。为想象中的未来预先架模式，是过度设计最典型的指纹。
3. **组合优先于继承**：需要复用结构或行为时，先想持有与委托，再想继承。策略、装饰器、桥接都是组合路线——它们存在本身就是这条实践的证明。
4. **针对接口编程，不针对实现**：调用方只认抽象，具体实现可整体替换。凡是调用方拿着具体类分支挑选的地方，都是这条实践的缺口。
5. **能用语言内建机制就不上完整模式**：现代语言里许多模式已经隐身——枚举分派、函数值当策略、内建事件。杀鸡用模式类结构，复杂度反噬。
6. **一个模式解决一个问题**：不叠罗汉。装饰器套装饰器再套代理、工厂里再建工厂——每层间接都必须有自己的单独理由，说不出来就拆层。
7. **间接层必须有回报**：每个模式都引入间接层，间接层的成本是看得见的（跳转、间接、调试），回报必须明确——可替换、可扩展、可隔离，三者至少占一。没有回报的间接层就是债。
8. **模式词汇即沟通语言**：用了模式就在命名上体现（叫 XXFactory、XXStrategy、XXAdapter），让读代码的人一眼认出意图。藏着的模式等于没用的模式。
9. **模式要随代码演进维护**：当年为三种实现架的工厂，如今只剩一种实现——拆掉；当年两级的责任链，如今永远单级直通——拆掉。模式僵化不拆，按维度 9 演进违规报告。
10. **缺模式与模式病同等扣分**：手写分派该用状态/策略而不用，扣；一种实现套抽象工厂，同样扣。尺子只认"复杂度与问题是否匹配"，不认"用没用模式"。

## 分类总表

| 类别 | 数量 | 解决的问题 |
|---|---|---|
| 创建型 | 5 | 对象怎么造：创建过程本身的封装与解耦 |
| 结构型 | 7 | 类与对象怎么组装：接口的转换、组合、简化 |
| 行为型 | 11 | 对象之间怎么协作：职责分配与通信方式 |

## 创建型（5 种）

### 1. 单例模式
- **意图**：保证一个类只有一个实例，并提供全局访问点。
- **正当场景**：完全自包含、功能单一、不含业务状态的基础设施（日志器、只读配置快照）。
- **误用信号**：业务对象（持有业务状态或输入输出）自立单例——单例成瘾就是对象组装失控，全局访问点本身就是公共耦合的入口。
- **评分器联动**：维度 10（对象组装）；正当边界与维度 10 的"基础设施例外"完全一致。

### 2. 工厂方法模式
- **意图**：定义创建对象的接口，让子类决定实例化哪一个——"造哪个"的决策从调用方移进类型体系。
- **正当场景**：同类对象有多种具体形态、且形态会新增。
- **误用信号**：只有一种实现也包一层工厂——多态没有兑现的抽象。
- **评分器联动**：维度 3（调用方不该自己挑选具体类再分支）；依赖倒置 + 开闭原则的直接落点。

### 3. 抽象工厂模式
- **意图**：创建**一族**相关对象而无需指定具体类（同一主题的一套）。
- **与工厂方法的区别**：一族对多个，一个对一个。
- **误用信号**：族内永远只有一种搭配——为单一组合建族。
- **评分器联动**：维度 1（跨模块的创建逻辑收口到一层）。

### 4. 建造者模式
- **意图**：把复杂对象分步构建，同一构建过程可产出不同表示。
- **正当场景**：构造参数爆炸（四五个以上、可选参数多、参数间有顺序依赖）。
- **误用信号**：两三个参数也上建造者。
- **评分器联动**：维度 8（必填约束在构建过程中收口，而不是散在运行时判空）。

### 5. 原型模式
- **意图**：通过克隆已有实例创建新实例，而不是从零构造。
- **评分器联动**：维度 2（封装）——外部逐字段手工拷贝造副本（批量字段拷贝），正是"该有语义克隆/派生副本"的信号；`记录.派生副本(目标)` 就是原型的语义化形态。

## 结构型（7 种）

### 6. 适配器模式
- **意图**：转换接口，使不兼容的类协同工作。
- **正当场景**：接入不可修改的外部代码（三方库、遗留系统）——在边界处转成自家接口。
- **误用信号**：自家代码之间也层层适配（该直接改接口，不是套壳）。
- **评分器联动**：维度 1（外部依赖在边界处适配，不渗入内部分层）。

### 7. 桥接模式
- **意图**：把抽象与实现拆成两个独立变化的维度（如"形状"×"渲染方式"），组合代替继承爆炸。
- **正当场景**：两个正交的变化点被硬编成一棵多层继承树。
- **评分器联动**：维度 7（正交变化点各自进配置/策略）；合成复用原则的落点。

### 8. 组合模式
- **意图**：树形结构组织对象，部分与整体用同一接口处理。
- **正当场景**：调用方对"单个元素"和"元素组"做完全相同的操作。
- **误用信号**：调用方到处 `if 是叶子 … else 是分支 …`——类型分派复述。
- **评分器联动**：维度 3（知识边界——叶子/分支的内部结构知识泄漏）。

### 9. 装饰器模式
- **意图**：动态给对象叠加职责，不改原类、不用继承。
- **正当场景**：职责可自由叠加组合（如层层包装的中间处理）。
- **误用信号**：职责组合其实固定——直接组合持有更简单。
- **评分器联动**：维度 2（用继承叠职责导致子类爆炸时，该用装饰器/组合）。

### 10. 外观模式
- **意图**：为子系统提供一个统一的高层入口，隐藏内部零件。
- **正当场景**：调用方只需要"做这件事"，不需要知道子系统内部有哪几个零件怎么配合。
- **评分器联动**：维度 3、5——Manager 的公开方法就是业务子系统的外观；调用方直接摸子系统内部零件 = 该有外观。

### 11. 享元模式
- **意图**：共享细粒度的相同实例以节省内存。
- **评分器联动**：性能不在评分范围——大量重复构造不可变小对象只作提示报告，不扣分。

### 12. 代理模式
- **意图**：控制对对象的访问（延迟加载、权限校验、远程代理）。
- **误用信号**：代理层只做透传（既不控制也不管理生命周期）。
- **评分器联动**：维度 8（不可信访问在代理边界处拦截——安全边界）。

## 行为型（11 种）

### 13. 责任链模式
- **意图**：请求沿处理者链传递，每个处理者自行决定处理还是交给下一个。
- **正当场景**：多级处理有先后、每级可拦截或放行（审批流、多层校验）。
- **误用信号**：处理路径固定且单一也搞成链。
- **评分器联动**：维度 5（业务流的多级处理显式成链，顺序与放行点可见）。

### 14. 命令模式
- **意图**：把请求封装成对象，从而可以排队、记录、撤销、重放。
- **正当场景**：操作需要被当作数据对待（任务队列、撤销栈、审计日志）。
- **评分器联动**：维度 4（操作对象化后进队列/进日志——数据流可追踪）。

### 15. 解释器模式
- **意图**：为一门小语言定义文法和解释器。
- **正当场景**：确有稳定的表达式/规则语言要解析。
- **误用信号**：几个简单判断也要造一套迷你语言——**过度设计高发区**。
- **评分器联动**：维度 9（演进健康）+ "简单"属性。

### 16. 迭代器模式
- **意图**：顺序访问聚合内容的元素，不暴露内部表示。
- **误用信号**：外部直接拿到内部集合并遍历操作——内部表示泄漏。
- **评分器联动**：维度 2、3（暴露内部结构给调用方）。

### 17. 中介者模式
- **意图**：把对象之间的网状交互收拢到中介者，网状变星型。
- **正当场景**：多个对象两两互相持有、互相调用，改一个牵一片。
- **评分器联动**：维度 4、10（对象纠缠是数据流不可追踪与组装失控的共同根源）。

### 18. 备忘录模式
- **意图**：保存并恢复对象的状态，而不暴露其内部结构。
- **误用信号**：外部为支持回滚，手工抄写另一个对象的全部字段——先泄漏了内部结构再谈恢复。
- **评分器联动**：维度 2（回滚应是对象自己提供的语义快照）。

### 19. 观察者模式
- **意图**：一对多依赖，目标状态变化时自动通知所有订阅者。
- **正当场景**：确实存在一对多的广播关系，且订阅方集合会变。
- **误用信号**：固定顺序的简单调用也发事件——多一层间接 + 时序变成黑盒；事件满天飞会把显式调用链搅成隐式网，**滥用反而伤害数据流可追踪**。
- **评分器联动**：维度 4（事件必须仍是可追踪的显式通道，不是甩锅通道）。

### 20. 状态模式
- **意图**：对象的行为随内部状态改变而改变，每种状态封装成独立类型——巨型条件分支的解药。
- **误用信号**：用一个可变标志字段+满屏 `if 状态 == X` 分支；标志硬编码取值。
- **评分器联动**：维度 3、7（按状态分支复述=知识泄漏；状态取值硬编码=策略未分离）。

### 21. 策略模式
- **意图**：封装一族可互换的算法，调用方面向抽象使用，具体策略可替换。
- **正当场景**：同一业务存在多种可切换的做法（不同计价、不同压缩、不同路由）。
- **评分器联动**：维度 7 的直接实现形态——配置选策略 + 缺省兜底，代码零策略；开闭原则的标准落点。

### 22. 模板方法模式
- **意图**：父类固定流程骨架，子类只填个别步骤。
- **正当场景**：多个类的流程骨架确实相同、仅步骤细节不同。
- **误用信号**：骨架其实无共用（该用策略/组合），或子类频繁覆盖骨架（骨架定错了）。
- **评分器联动**：维度 5（业务流骨架固定、步骤可变时，骨架即编排点）。

### 23. 访问者模式
- **意图**：在不修改元素类的前提下，为整族元素添加新操作。
- **正当场景**：元素结构稳定、操作频繁新增（双分派）。
- **误用信号**：元素结构简单或频繁变——访问者的双分派复杂度反噬；为幻想中的扩展预先架访问者。
- **评分器联动**：维度 9（过度设计指纹）。

## 总映射表（模式 × 主要支撑原则 × 评分维度）

| 模式 | 主要支撑的原则 | 评分维度联动 |
|---|---|---|
| 单例 | 封装 | 10 |
| 工厂方法 | 依赖倒置、开闭 | 3 |
| 抽象工厂 | 依赖倒置 | 1 |
| 建造者 | 封装 | 8 |
| 原型 | 封装 | 2 |
| 适配器 | （边界隔离） | 1 |
| 桥接 | 合成复用 | 7 |
| 组合 | 里氏替换 | 3 |
| 装饰器 | 合成复用 | 2 |
| 外观 | 迪米特法则 | 3、5 |
| 享元 | （性能，不计分） | — |
| 代理 | 依赖倒置 | 8 |
| 责任链 | 开闭 | 5 |
| 命令 | 单一职责 | 4 |
| 解释器 | 开闭 | 9（误用重灾区） |
| 迭代器 | 迪米特法则 | 2、3 |
| 中介者 | 单一职责 | 4、10 |
| 备忘录 | 封装 | 2 |
| 观察者 | 开闭 | 4（防滥用伤数据流） |
| 状态 | 开闭、里氏替换 | 3、7 |
| 策略 | 开闭 | 7 |
| 模板方法 | 里氏替换 | 5 |
| 访问者 | 开闭 | 9（误用重灾区） |
