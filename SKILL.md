---
name: code-quality-scorer
description: Use when 需要对一个软件项目/代码库/模块的代码质量打出 0-100 分、做质量评级或项目间对比时。评架构分层、封装、数据流、模块边界与可测性，与编程语言无关、与业务实现无关。
---

# 代码质量评分器（0-100）

## Overview

给任意软件项目打一个 0-100 的质量总分。只评**代码是怎么组织的**（分层、封装、数据流、模块边界、演进健康），不评业务功能对错、不评算法优劣、不评性能。规则与编程语言无关——任何语言的项目用同一把尺子。

理想代码的四条标准：

1. **层次分明**：通用架构层（脚手架、util、helper 等无业务工具）→ 业务通用层（各模块共用的 model 及其简单业务封装）→ 详细业务层（封装彻底、最小暴露面）。
2. **数据流清晰**：数据在模块之间显式、可追踪地传递。
3. **业务流清晰**：一个业务动作从头到尾的步骤序列可读、可追溯，有明确编排点。
4. **每个模块可抽象为"输入 → 处理 → 输出"**；边界清晰是可测试的前提——边界不清的模块写不出好测试用例。

详细业务层的内部理想态：**封装好的底层业务函数的排列组合**——上层代码读起来是业务步骤序列，不是数据操作序列。

本评分器最终评的是教科书级代码质量属性：**易读、易维护、易扩展、简单、灵活、易测试、易移植、安全边界**。下面的结构维度是检查手段，质量属性是评分目的——每个结构违规最终都落到某个质量属性的损害上。

而这一切归根结底是同一件事：**继承、封装、多态、抽象**四大基本要素的运用水平。维度和属性只是要素在工程上的投影；理解了要素，每个违规"为什么是违规"都不需要死记。

## 四大基本要素（一切维度的根基）

### 封装——四大要素之首，其余三要素的载体

**是什么**：把数据和操作这些数据的行为绑在一起，由对象自己管理自己的状态；对外只暴露必要的语义接口，藏住实现细节。它有两层：①数据与行为绑定——属性是业务原料，消费者应是它自己的方法，不是外部类；②暴露面控制——接口能小则小，公开的每一个字段、方法、类型都是与外界的耦合点。

**为什么是根基**：没有封装，继承继承到的是一堆散乱字段，多态没有稳定的接口可供替换，抽象没有落点。封装失败的两种形态，恰好是本评分器最重的两类违规：散装数据（数据和行为分离，外部类逐字段读写别人的数据结构）和知识泄漏（调用方必须懂内部规则才能选对调用）。暴露面每扩大一分，调用方与被调方的耦合就深一分——**改动的传播半径等于暴露面**。最小暴露面不是风格洁癖，是把改动爆炸半径压到最小的工程手段。

**在本评分器中的体现**：维度 2（封装彻底、最小暴露面）的主干；维度 3（调用方知识边界）的失败本质就是封装被穿透；维度 8（校验放对位置——不变量的守护是封装的职责）；安全边界属性（暴露面即攻击面）。

### 抽象——分层的语言

**是什么**：从具体中提炼本质、忽略无关细节，用"是什么、做什么"代替"怎么做"。它有三个层级：方法级（语义函数：`记录.派生副本()` 而非十个字段的拷贝）、模块级（输入→处理→输出，处理过程是黑盒）、系统级（三层结构本身就是三级抽象：无业务工具 → 共用模型 → 具体业务）。

**判据**：上层代码读出来应当全是"做什么"（业务语义），见不到"怎么做"（实现细节）。命名是抽象的最小单位——技术性命名（`doStep2`、`handleCaseB`）就是抽象漏了，业务性命名（`重试已排期`）才是抽象到位。**统一语言**：代码对概念的称呼必须与业务方对该概念的实际称呼一致——业务方说"工单"，代码里叫 `TaskInfo`，每个读者都在心里做翻译，就是抽象失真。

**在本评分器中的体现**：维度 1（层次分明=抽象的层级化落地）；维度 5（业务流清晰=业务步骤以抽象语义排列）；维度 7（策略与代码分离=把"会变的"抽象进配置）；维度 3（意图枚举参数就是抽象证据）。抽象失败的一贯形态：实现细节漏进调用点、调用方代码里复述被调方的内部规则。

### 继承——归类与契约，慎用的强耦合

**是什么**："是一个"（is-a）关系：子类获得父类的语义与契约，形成类型的概念层级。继承解决归类与复用：共性的东西上移到父类/共用层，差异留在子类。

**必须深刻理解的一面**：继承是**白盒复用、最强耦合**——为"拿到实现"而继承，比不继承更糟。判据有两条：①is-a 是否真的成立（"购物车是一个列表"不成立，"信用卡支付是一种支付方式"成立）；②子类是否无条件遵守父类的语义承诺——任何"用父类的地方换成这个子类会出错"的情形，都是继承关系本身错误。因此组合优先于继承不是否定继承，是承认它的耦合强度：能通过"持有并调用"复用的，不要用"变成它"来复用。

**在本评分器中的体现**：维度 1（业务通用层的共用 model 及其简单封装，就是"归类上移"的正确落点）；维度 2（继承边界即暴露面边界——哪些对子类开放是封装决策）。注意：本评分器不把"继承用得多"当加分项，只检查"用得对不对"。

### 多态——替换不伤调用方的机制

**是什么**：同一个语义接口，多种具体实现；调用方面向"做什么"编程，"谁来做"可以整体替换。多态是"新增一种情况，不修改任何调用方"的机制保障——这正是易扩展的物理学。

**失败形态**：调用点散布 switch/if 复述类型分派（"如果是 A 类就走这条路，如果是 B 类就走那条路"）——每加一种新情况，所有调用点都要改，漏改一处就出 bug。这就是维度 3 点名的"多个同级方法让调用方挑"的深层病因：多态缺位，知识只好泄漏。

**修复形态恒为**：分派内移（被调方内部用统一入口分派）或接口化（调用方只认接口，实现可整体替换）。模块可测试也依赖它：能替换才能用假实现打桩——多态到位的模块，测试不需要拉起真实依赖。

**好品味（比分派更根本的一步）**：多态消灭的是"调用方复述分派"，好品味连分派本身都消灭——数据结构选对了，特殊情况不存在，代码自然覆盖全部情形。经典对照：链表删除对头/中/尾写三段特判是差品味；用间接指针统一处理，特判消失、代码减半。评任何带特判的代码都问一句：**这个特判能不能被更好的数据结构/抽象整个消掉？**特判是"用代码量买兼容"，好抽象是"用设计买兼容"。

**在本评分器中的体现**：维度 3（单入口分派）、维度 6（模块边界与可测性）、维度 7（策略对象可替换）；灵活、易扩展、易测试三个属性的直接支撑。

### 归根结底：两个映射

**结构维度 ← 要素支撑**：

| 维度 | 主要支撑要素 |
|---|---|
| 1 层次分明 | 抽象（分层=分级抽象）、继承（归类上移到共用层） |
| 2 封装最小暴露面 | 封装 |
| 3 调用方知识边界 | 封装（知识泄漏=封装被穿透）、多态（分派缺位=泄漏的病因） |
| 4 数据流清晰 | 封装（状态收进对象、不留全局） |
| 5 业务流清晰 | 抽象（业务语义编排） |
| 6 模块边界与可测性 | 多态（可替换才可模拟）、抽象（输入输出黑盒化） |
| 7 策略与代码分离 | 抽象（把会变的抽象出去）、多态（策略可替换） |
| 8 校验放对位置 | 封装（不变量在结构定义处守护） |
| 9 演进健康 | 封装+抽象（无收敛的补丁堆=两要素持续失守） |
| 10 对象组装 | 封装（组装关系显式持有，不靠全局纠缠） |

**质量属性 ← 要素到位**：

| 质量属性 | 靠什么要素撑起来 |
|---|---|
| 易读 | 抽象（读到的是整齐的代码，没有多余空格和空行） |
| 易维护 | 封装（改动bug不扩散原来的业务代码） |
| 易扩展 | 多态+抽象（加功能不大范围改代码） |
| 简单 | 抽象（选择最优雅的方案进行实现，任何人一眼就能明白和理解，减少阅读成本，不绕） |
| 灵活 | 多态（以最少的代码量，兼容多种变化的可能性） |
| 易测试 | 封装+多态（边界清晰、依赖可模拟） |
| 易移植 | 抽象+封装（层隔离，换底座只动底层） |
| 安全边界 | 封装（暴露面=攻击面） |

## 面向对象七大设计原则

四大要素是材料，七大原则是运用这些材料的工程法则——它们评价面向对象设计的好坏、指导封装与解耦。七条不是并列清单而是一张网：单一职责划边界 → 接口隔离与最少知识压暴露面 → 里氏替换与合成复用管住继承 → 依赖倒置与开闭管住扩展方向。

| 原则 | 核心思想 | 违规信号（工程形态） | 评分器落点 |
|---|---|---|---|
| 单一职责原则 | 一个类只负责一个职责；职责越单一，封装边界越清晰 | 一个类干几件不相关的事；一个业务变化迫使多个不相关类同时修改 | 维度 1、6 |
| 开闭原则 | 对扩展开放、对修改关闭；通过抽象封装变化点 | 新增一种情况必须回头修改既有调用点的分支判断，而不是新增实现 | 维度 3、7 |
| 里氏替换原则 | 子类能无条件替换父类且不破坏程序；继承不许破坏父类的封装契约 | 子类悄悄收窄行为或抛"不支持"；调用方要先认子类型才敢调用 | 四大要素·继承节 |
| 依赖倒置原则 | 依赖抽象，不依赖具体实现；高层模块通过接口使用低层，隐藏实现 | 高层直接依赖低层具体类；换一个实现要改上层代码 | 维度 1、6 |
| 接口隔离原则 | 用多个专门接口，不用一个胖接口；接口也要封装，只暴露必要方法 | 实现类被迫实现一堆用不到的方法；调用方拿到远超所需的方法面 | 维度 2 |
| 合成复用原则 | 优先用组合/聚合，少用继承；组合更能保持封装性、降低耦合 | 为复用实现而继承（"有一个"被写成"是一个"） | 四大要素·继承节 |
| 迪米特法则（最少知识原则） | 少和其他对象打交道；只暴露必要接口，隐藏内部细节 | 链式穿透取值；调用方必须懂被调方内部规则才能选对调用 | 维度 3（本评分器对其做了两层扩展：语法穿透+知识边界） |

## 模块独立性：内聚与耦合双标尺

模块独立性用两把标尺综合评判：**内聚**（模块内部元素彼此关联的紧密程度，越高越好）与**耦合**（模块之间联系的紧密程度，越低越好）。评任何模块都要两头判：内部看内聚级别、对外看耦合级别。理想模块 = **功能内聚 + 数据耦合**。

### 耦合标尺（模块之间，越低越好）——七级从好到差

| 级别 | 形态 | 定性 |
|---|---|---|
| 非直接耦合 | 两模块无任何联系 | 完全独立 |
| 数据耦合 | 仅传递简单数据值（参数进、结果出） | **目标形态**——模块间联系应保持在这级，也是"输入→处理→输出"的自然形态 |
| 标记耦合 | 传递整个数据结构（数组名、记录名），对方只用其中一部分 | 中间态：把整个结构递进去 = 对方看见了全部字段，用到的越少知识泄漏越重；修复 = 收窄为只传所需，或改为语义函数调用 |
| 控制耦合 | 传递控制变量（开关、标志、模式号），指挥对方走哪条分支 | **违规**：调用方必须懂对方内部分支才知道该传什么——"调用方知识边界"的直接形态 |
| 外部耦合 | 多模块访问同一全局简单变量 | **违规**：数据走隐式全局通道 |
| 公共耦合 | 多模块读写同一公共数据环境（全局数据结构、全局可变状态） | **严重违规**：谁都能改、改了谁都不知道——数据流不可追踪的根源 |
| 内容耦合 | 直接访问另一模块的内部数据或代码 | **最严重**：封装的彻底失守 |

### 内聚标尺（模块内部，越高越好）——七级从好到差

| 级别 | 形态 | 定性 |
|---|---|---|
| 功能内聚 | 模块内所有元素共同完成**一个**单一功能，缺一不可 | **目标形态**——单一职责原则的达成形态 |
| 顺序内聚 | 前一元素的输出是后一元素的输入，流水线式 | 可接受 |
| 通信内聚 | 各功能操作同一份数据，但功能之间无先后、无依存 | 中间态：共享数据的多功能应拆成各自功能内聚的模块 |
| 过程内聚 | 元素按同一控制流顺序执行，但彼此无数据传递 | 偏差：按流程切模块，不是按功能切 |
| 时间内聚 | 元素只因执行时机相同而放一起（如把所有初始化堆进一个 init） | **违规**：时机不是功能 |
| 逻辑内聚 | 几个逻辑相似的功能塞进一个模块，靠传入开关/标志选择执行哪个 | **违规**：控制耦合的接收端——调用方必须懂内部分支才知道传什么 |
| 偶然内聚 | 元素之间毫无关联，只为省事堆在一起（万能工具大类） | **最差**：无内聚可言 |

### 双标尺综合应用规则

- 评审目标：每个模块**功能内聚 + 数据耦合**（顺序内聚可接受）。
- 耦合侧：**内容耦合、公共耦合**出现即至少按"普遍"档起步扣（40-60），成片存在按"系统性"（70-100）；**控制耦合、外部耦合**按波及范围套三档幅度；**标记耦合**（传整个结构只用少数字段）按知识泄漏报告，不按风格问题报告。
- 内聚侧：**偶然内聚、逻辑内聚、时间内聚**出现即记违规；过程内聚、通信内聚按"模块边界切错刀"报告。
- 判级挂钩的维度：维度 2 封装（内容耦合=极端形态）、维度 3 知识边界（控制耦合、标记耦合、逻辑内聚）、维度 4 数据流（外部耦合、公共耦合）、维度 5 业务流（时间内聚——步骤只因时机相同而堆装）、维度 6 模块边界（双标尺判级结果是边界质量的主要证据）、维度 1 层次分明（偶然内聚=工具层垃圾抽屉）。
- 与四大要素的关系：耦合级别的恶化路径，就是封装逐步失守的过程——数据耦合（只见接口）→ 标记（见到全部数据）→ 控制（必须懂内部逻辑）→ 公共/内容（直接进屋翻东西）。内聚级别的恶化则是抽象失守的过程——功能内聚（一个清晰的名字说得清它做什么）→ 时间/偶然（只能用"杂项""通用"命名，说不清单一是干什么的）。

## 经典 23 种设计模式（详细论述见同目录 design-patterns.md）

设计模式是四大要素 + 七大原则在常见场景下的标准解法。评分时的**双向判据**：

- **缺模式 → 扣分**：手写 switch 分派（该用状态/策略模式）、全局单例纠缠（该用入口组装/中介者）、巨型条件分支复述类型（该用多态）——按对应维度记违规，修复方向直接指名模式名。
- **模式病 → 同样扣分**：只有一种实现却套抽象工厂、无状态变化却套状态模式、为幻想中的扩展点加解释器/访问者——按"简单"属性受损与维度 9（过度设计）记违规。**模式是缺失时的解药，不是默认配料。**

三类速记：创建型 5 种（单例/工厂方法/抽象工厂/建造者/原型——对象怎么造）、结构型 7 种（适配器/桥接/组合/装饰器/外观/享元/代理——类怎么组装）、行为型 11 种（责任链/命令/解释器/迭代器/中介者/备忘录/观察者/状态/策略/模板方法/访问者——对象怎么协作）。

逐个模式的意图、正当场景、误用信号、维度联动与总映射表：**读 design-patterns.md**（评分涉及"该不该用某模式"的判断时必须先读它，不许凭印象裁）。

## 评分框架

**判断先行，算术退位。** 评分流程三步：

1. **通读定性**：读完代码先形成整体判断——这个项目落在哪个段位（见分数语义锚点）。问自己：这套代码拿给一个资深工程师看，他会不会说"写得规范"？结构方向对不对？问题是个别的还是成片的？
2. **段位校验**：用下面的维度清单核对两件事——有没有漏看的结构性问题（该降段）；有没有把行业惯例、点状小瑕疵当成结构问题（该升段）。惯例从轻、点状不上纲、一次计罪三条规则在这一步执行。
3. **给分**：总分由判断给出，落在段位内。维度表是**证据的组织形式**（解释为什么是这个分、差在哪、好在哪），不是加权算术的输入——**禁止机械加权出总分**。权重列只表示"哪个维度的问题更值得在报告里强调"。

| # | 维度 | 权重（报告强调度） | 一句话规则 |
|---|---|---|---|
| 1 | 层次分明 | 15 | 三层各归其位：工具层无业务、共用 model 集中、业务实现收敛在业务层 |
| 2 | 封装彻底、最小暴露面 | **20** | 字段封装为语义函数对外服务；暴露面不大于必要——**分数第一主维度** |
| 3 | 调用方知识边界 | **15** | 调用方不许被要求懂内部规则才能选对调用 |
| 4 | 数据流清晰 | 10 | 数据显式传递、来去可追踪，不走隐式全局通道 |
| 5 | 业务流清晰 | 7 | 业务步骤序列可读可追溯，有明确编排点，异常路径显式 |
| 6 | 模块边界 | 10 | 每个模块能抽象为输入→处理→输出；只看代码设计，永不看测试用例 |
| 7 | 策略与代码分离 | 5 | 可变语义（阈值/开关/等级规则）一律进配置，代码零策略 |
| 8 | 校验放对位置 | 3 | 必填靠类型声明、版本靠版本号协商、格式约束靠结构定义 |
| 9 | 演进健康 | 5 | 局部补丁不许替代体系级能力；无评审轮次注释堆；同一决策不许多份表达 |
| 10 | 对象组装 | 10 | 业务对象在程序入口统一创建传递；业务对象不许自立全局单例 |

### 维度分两级：结构主维度与内务维度

- **结构主维度（1 层次、2 封装、3 知识边界、4 数据流、6 模块边界、10 组装）**——决定分数水位。模块边界清晰、封装收口到位 = 85 基线起。**结构形态重扣条款**：以下每类从基线扣 12-18 分，可累计压到 50 以下：知识泄漏成默认写法（多入口同级暴露/调用点复述内部规则）、model 散布各层、业务渗入工具层、跨服务共享上下文无收口（散装字段被多模块交叉读写）、假分层（层答不出存在理由）、业务状态单例组装失控成群。
- **内务维度（7 策略分离、8 校验位置、9 演进健康）**——**合计拉低总分不超过 10 分，且限幅只保护"有收敛意志"的内务**。手抄重复多少份、评审注释多少处、配置缺几件、校验放错几层——全部进 P1-P3 报告（修复指引效力不变），但不许把分数拉穿。内务问题升级为结构问题时计入水位，升级判据（满足其一即升级）：
  - **零收敛的补丁堆**：评审轮次注释成山（密度高于业务注释）+ 同一决策多份平行展开/拷贝 + 全库无一次架构收敛动作（无迁移路径、无正解在位、无收敛重构）——这是"逐轮局部最优的堆叠"，封装与抽象持续失守的形态，按结构重扣计，**不受限幅保护**。对照：同样有重复与注释，但存在 DEPRECATED 迁移路径、模板正解已在库中、有收敛重构痕迹的，属"演进中的内务"，受限幅保护。
  - **配置管理做成了一份文件的解析器+补丁堆**（体系缺失：配置源散落多处、无中心持有者、读侧无快照无版本）——计入结构。
  - **模板代码成规模且零收敛**（如属性名手写三遍占整层近两成行数、上百行成员逐一拷贝的构造成片）——计入结构。

### 质量属性面板（结构维度的投影）

总分之外，按映射把结构维度分投影为教科书质量属性分（每个属性 = 其映射维度得分的平均），用于定位"这个项目差在哪个质量目标上"：

| 质量属性 | 支撑它的结构维度 |
|---|---|
| 易读 | 4 数据流清晰、5 业务流清晰 |
| 易维护 | 1 层次分明、2 封装、3 知识边界、9 演进健康 |
| 易扩展 | 2 封装、3 知识边界、6 模块边界可测、7 策略分离 |
| 简单 | 8 校验放对位置、9 演进健康（无过度设计） |
| 灵活 | 7 策略与代码分离、10 对象组装 |
| 易测试 | 4 数据流清晰、6 模块边界可测、10 对象组装 |
| 易移植 | 1 层次分明（通用层与业务隔离）、7 策略与代码分离 |
| 安全边界 | 2 封装（最小暴露面）、8 校验放对位置（边界处拦截不可信输入） |

### 简单与灵活：必须统一而非取舍的矛盾（质量属性的枢纽）

表面上这对属性互相否定：**简单意味着代码少 → 代码少意味着兼容面窄 → 兼容面窄就是不灵活；反过来，灵活要求兼容多种变化 → 每种情况都要代码去接 → 代码多就是不简单。** 恰恰因为这对矛盾存在，把它处理好的代码才有质的提升——简单与灵活不是二选一的取舍，是同一个设计高度的两面。

矛盾成立的隐含前提是：**兼容能力由代码量购买**——每个特例写一段判断、每个等级写一个入口方法、每个客户写一个分支。这种写法下两个属性确实此消彼长，而且是双输：代码又多，兼容面又锁死（加第四种情况还得再加第四段代码）。

化解矛盾的途径只有一条：**兼容能力不由代码量决定，由"变化被抽象的维度"决定。** 把变化从控制流里赶出去，赶进数据、类型、结构——控制流保持最短（简单），变化容量装在抽象维度里（灵活）：

1. **变化数据化**：规则、阈值、等级进配置表，引擎一个循环通吃——代码量恒定，兼容面 = 表的行数。
2. **变化类型化**：调用方面向接口写一次，新情况 = 新增一个实现——调用方零改动，兼容面 = 实现数。
3. **原语正交化**：少量正交的核心原语，靠组合表达万千行为——原语少到一眼看尽（简单），组合容量指数级（灵活）。核心简单支撑表层复杂。
4. **特例统一化**（好品味）：选对数据结构，边界情况被算法自然覆盖——特判消失、代码减半，兼容面反而从"逐点处理"变成"全域覆盖"。

评分判据（判定这对矛盾的处理水平）：**"新增一种情况，既有代码要改几处？"**

- **0 处**（新增一条数据/一个实现即可）：矛盾已统一——质量质变的标志
- 1-2 处局部改动：合格
- 多处散改（新情况触发霰弹式修改）：在用代码量买兼容——简单与灵活双失，按维度 3、7、9 联合报告

一句话：**差的代码简单与灵活双失（代码多且难扩展），好的代码简单与灵活双得（代码少且易扩展）——分水岭只有一个：变化是被抽象了，还是被硬编了。**

### 各维度详规则与违规信号

**1. 层次分明（15）**

三层各自的归属与违规信号：

- **通用架构层**（脚手架、util、helper 等工具）：与业务无关。违规：业务逻辑渗入工具层；工具函数里出现项目业务概念。
- **业务通用层**：不同模块共用的 model 及其**简单**业务封装，集中定义。违规：model 跟着功能就近定义、散落各处，"项目有哪些数据结构"没有唯一去处；共用 model 里塞了复杂编排（复杂编排属于详细业务层）。
- **详细业务层**：具体业务实现，实体统一 xxxManager 形态（按模块/功能/类型划分皆可，言之有理即可）。违规：散件类遍地——xxxWatcher / xxxResolver / xxxLoader / xxxExecutor 等业务行为类各立门户，没有统一形态和治理点；本该共用的 model 囤在单个业务模块里不出门。

判深标准与文件级规则：

- **分层是职责不是名词**：目录按文件种类分（所有协议一堆、所有实现一堆）而每层答不出"为什么存在"= 假分层。总评要查**贯穿主线**——配置、事件、依赖各走各的散装路线、无统一入口的，整体架构按不成立评。
- **文件级单一职责**：巨型文件混多种职责（一个文件同时干结构定义+数据迁移+读写+维护）= 层内职责失守。一文件一主类型，文件名 = 主类型名。
- **命名即文档**：全库统一项目前缀（一眼可辨"这是本项目的代码"）；后缀表角色——Manager=有状态业务实体、Util=无状态静态工具、Config/Event/Snapshot=数据角色；**禁止语义空洞后缀**（Helper/Bridge/Wrapper/Core/Handler——说不清它是什么，只说得清它包着谁）。

依赖方向规则：

- **循环依赖**：模块间出现环 = 边界失守的直接证据，无论其他方面多讲究，按严重违规报。
- **稳定依赖方向**：稳定、被广泛依赖的组件不许反过来依赖频繁变更的组件；业务逻辑不许直接依赖具体基础设施（该经过抽象）。
- **数据结构优先的评审起点**：动手评代码前先看数据模型——数据结构及其关系是否清晰到"代码自然形成"。差的代码先写代码再补数据结构（特例丛生）；好的代码数据模型立住了，代码是它的自然投影。数据关系混乱的，按根子上的问题报，不按表层代码风格报。

**2. 封装彻底、最小暴露面（15）**

心法：**封装、封装、还是封装**——本评分器一切细节纪律的归宿都是封装；拿不准某段代码怎么评时，先问"这里封装到位了吗"。

- **资源所有权——谁分配、谁释放**：分配资源的一方负责释放到生命终点，或把所有权显式移交（交接后原持有者不再碰）；裸资源（指针/句柄/描述符/连接）跨边界传递而无所有权约定 = 泄漏与双重释放的温床。调用方收到裸资源却不知道"要不要关、能不能关"即违规。
- 违规信号：外部类逐字段读取另一个数据结构做业务判断（链式取值一串读）、批量字段拷贝、字段被跨类直接赋值修改；公开面大于必要——能收私有的方法/类型/字段公开了，模块内部结构泄漏到外部。
- **暴露面即隐性契约**：调用方足够多时，一切可观察行为（含实现细节、错误信息文本、巧合的调用顺序、未声明的副作用）都会被某个调用方依赖上。暴露出去的每一样东西都是收不回来的承诺——这是最小暴露面最硬的理由。
- **对外契约纪律**：对外发布的接口一旦发布即承诺，不许可变（要改必须走"警告 → 迁移路径 → 兼容期"）；内部接口随便改。判据：升级被调方导致调用方坏 = bug 在被调方，不赖调用方用得刁。
- 修复方向：字段收敛为只读暴露（或语言等价机制）+ 语义函数（如 `规则.匹配(事件)`、`记录.派生副本(目标)`）；详细业务层尽可能封装彻底，把暴露面压到最小。

**外部系统封装判则**（内核接口、三方库、驱动、外部协议都适用——把外部世界的类型完整关在门内，门外只见业务对象）：

- **类型边界**：外部系统的原始类型零外泄；"原始消息 → 业务对象"的完全转化在封装内部完成，下游不该 import 外部世界。
- **单出口**：对外只有一个回调/一个入口，事件类型是业务对象的字段，不是接口的分叉维度——不按外部系统的分类法拆多个处理方法、不搞处理者数组多播。
- **内聚三件**：资源生命周期（持有/拷贝时机/内存上限）、字段解析归一化、订阅与线程调度——全部内部管理。
- **热路径纪律**：外部回调线程做最少的事——粗过滤 + 拷贝入队即返，重活全部离线做。
- **半成品识别**：外围零件齐（错误码处理全、统计有）但核心责任缺（类型转化推给下游、生命周期不管、回调线程无保护同步遍历）——外围齐全不减罪，按核心缺失计违规。

**3. 调用方知识边界（15）**
- 两层检查，缺一不可：
  - 语法层：一长串链式取值穿透（a.b.c.d）——这只是最浅表象。
  - 知识层（本质）：调用方必须了解被调方内部分级/分流/合并规则才能选对方法。典型：多个同级方法让调用方二选一/三选一（按大小/等级分流的多个入口），调用点散布着复述内部规则的条件判断——知识泄漏进每个调用点。
- 耦合判级：传开关/标志/模式号控制对方分支（控制耦合）、传整个数据结构只用少数字段（标记耦合）均为本维度的直接违规形态。
- 修复方向：单入口 + 证据/意图枚举参数，分派规则内移为私有。

**4. 数据流清晰（10）**
- 违规信号：数据靠全局可变状态/隐式通道流动（A 写全局、B 偷读）——即外部耦合/公共耦合；同一份数据有多个入口改写；追一条数据"从哪来、到哪去、被谁改过"要跨多个文件来回跳。
- 修复方向：数据沿调用链显式传递（参数进、结果出），一跳可追踪，模块间保持数据耦合级。
- **显式优于隐藏**：隐藏的控制流（框架魔法回调、隐式拦截、全局劫持）、隐藏的资源分配（声明看着无副作用、底下悄悄建连接开文件）、隐藏的依赖关系 = 违规信号。读代码的人看见什么就该发生什么。
- 并发模型四句诀（多线程项目按此判）：**入口无锁入队、决策串行独占、跑腿并行有界、出口串行保序**。有状态的决策/处理核心串行独占是最简单正确的模型（不为其强上并发）；输入输出密集的杂务并行但要带背压上限（防下游打满）；输出保序（同一对象的先后顺序常是审计语义）；入口线程做完最少的事立刻返回。
- **锁序纪律**：需要同时持有多把锁时，加锁顺序必须全库固定统一（如按地址序、按声明序）——任意两处以相反顺序获取同一对锁就是死锁环，一处即 P1 级。
- **线程退出通道**：新开的每个线程必须有明确的退出通道（可停止标志、可 join、有超时的等待）——裸启动、无法通知停止、永远运行的线程 = 生命周期失控；停止路径要可达且干净（排空队列、释放资源后再退出）。

**5. 业务流清晰（10）**
- 一个业务动作从头到尾的步骤序列应当可读、可追溯：在哪触发、经过哪些步骤、失败在哪一步、怎么回退，读上层编排代码就能看懂。
- 违规信号：业务流程被切成碎片散落多处，没有一个编排点能从头读到尾；步骤先后靠副作用或全局状态变化的隐式顺序推进；异常路径不可见（错误被吞、就地散处理、找不到失败会走到哪）；上层代码读起来是数据搬运，不是业务步骤。
- 统一输出口：多路输出（存储/实时回调/日志/上报）应有统一的出口抽象——新出口 = 新增一个出口子类，处理层零改动；各路输出各自为政（回调裸函数、日志独立管道、存储直写互不相认）= 无出口抽象，按开闭原则违规报。
- **复杂流程步骤标注**：超过一个屏的复杂业务函数，必须用编号步骤注释标清处理流程（"// 1. 解析 → // 2. 校验 → // 3. 落库"）——流程要在阅读层可见，不该靠调试器才能理清。
- 修复方向：业务流程收敛到明确编排点（Manager 的方法即步骤序列），步骤显式调用、顺序显式、异常路径在流程里显式出现。

**6. 模块边界（10）**
- 每个模块应能抽象为**输入 → 处理 → 输出**。
- 违规信号：模块没有清晰输入（处理过程就地读外部/全局状态）、没有清晰输出（结果写向四面八方）；同一职责散在多个模块。
- 判定法：能否为这个模块独立构造输入、断言输出、不惊动其他模块？不能即违规。模块间联系逐对标耦合级：边界质量的主要证据就是判级结果。
- **铁律：tests 用例不读、不评**——评分时根本不打开测试文件（不读内容、不数文件、不评质量、不当证据、不当反证）。评价对象是生产代码本身。可测性只从代码设计面看：接缝、注入点、依赖纠缠、全局状态。模块能不能被测是代码设计出来的属性，与测试写没写无关。

**7. 策略与代码分离（10）**
- 违规信号：阈值、开关、重试次数、等级规则等可变语义硬编码在代码里。"这个值一直没变过"不成立——可变语义的可变性由语义本身决定，不由历史决定。
- 半配置化掺水：声称数据驱动/策略外置，但代码里钉死关键规则标识或取值（规则 id 写死在引擎、关键路径绕过配置直读）= 数据驱动掺水，按本维度违规计。
- 配置管理是**体系能力**，不是"一份配置文件的解析器 + 补丁堆"：统一写入口（本地文件/远程下发/编程注入多源归一）、读侧拿不可变快照、递增版本号可确认生效、变更可订阅。配置源散落多处、无中心持有者、运行参数全程静态不可调 = 体系级能力缺失。
- 修复方向：进配置 + 缺省值兜底，代码里不出现魔数。

**8. 校验放对位置（5）**
- 违规信号（防御放错层）：必填字段靠运行时判空（该靠非可空类型声明）；版本兼容靠逐字段嗅探（该靠版本号字段协商）；格式约束靠手写校验函数（该放进结构定义）；不可信输入不在边界处一次拦截、放任污染向内部扩散（安全边界违规）。
- **不可能状态不可表示**：非法状态应在类型/结构定义处就无法构造（用设计消灭错误），而不是处处防御性检查去兜。满屏防御性判空 = 状态机设计失败的事后买账。
- 定性：这不是"防御性编程"，是校验没放对层。

**9. 演进健康（5）**
- 违规信号：
  - 注释堆评审轮次/需求单号溯源（"Round 7 #3142"、"评审第 3 轮修改"）——每处局部合理、整体臃肿、没有一轮做架构收敛；
  - 体系级能力被局部补丁替代：配置源散落多处、唯一被认真对待的一份配置得到一个专属解析器——把"配置管理"做成了"一份文件的解析器+补丁堆"；
  - 手写模板代码在语言机制可消除时不消除：同一属性名手写三遍（声明/构造/序列化映射）、上百行成员逐一拷贝的构造——模板不消，每轮演进都在重复付税；
  - **知识重复**：同一决策在多处表达（同一规则两份实现、同一实体多种命名 user/account/member 指一个东西、必须同步改动的平行结构）——重复的对象是**决策**不是代码行，两份拷贝必然静默漂移，漂移即 bug。
- 判定法：评审编号密度高于业务注释密度 = 命中。

**10. 对象组装（5）**
- 违规信号：业务对象（持有业务状态或输入输出的）自立全局单例、互相之间靠全局访问纠缠；组合根自身也是单例（程序入口应持有唯一实例，而不是自己给自己提供全局访问点）。
- **注入方式**：持续服务依赖用对象/接口注入，闭包只适合一次性通知——把持续服务查询做成闭包注入，依赖关系就藏进闭包体（初始化签名不可见）、热路径多一层间接、业务逻辑散落到组装点。判据：这个依赖每次事件都要调（持续服务，该是方法调用）还是只通知一次（可用闭包）？
- 修复方向：程序入口统一创建（组合入口模式），作为成员传递。
- 允许例外：完全自包含、功能单一、不含业务状态的基础设施（如日志器）可以单例。

### 微观检查（横切清单——评任何维度时对视界内的函数顺带执行）

微观违规不设独立维度，按归属计入对应维度。

**函数形态**：

- 函数干一件事；超过一屏读不完、嵌套超过 3 层 = 认知过载 → 计入维度 5（上层读不出业务步骤）
- **声明序 = 实现序**：头文件/接口里函数的声明顺序必须与实现文件中的定义顺序一致——按声明找实现不该需要跳跃；实现序与声明序两张皮 = 文件内部无序，计入维度 1
- **无多余空格与空行**：空行只用于语义段落分隔（函数间、逻辑块间），同一段逻辑中零散的空行、行尾多余空格、连续多空行 = 视觉噪音，计入维度 5（读起来不整洁）；空格空行"除非有必要"才加
- **标志参数**：布尔/模式参数让一个函数按值干两件根本不同的事 = 函数有两个职责 → 计入维度 3（控制耦合的变体）
- **基本类型偏执**：领域概念裸用原始类型（邮箱就是字符串、金额就是数字，调用方自己分辨哪个字符串是邮箱）→ 计入维度 8（该用类型定义概念）
- 魔法数字、无意义命名（`d`、`tmp2`、`flag`）→ 计入维度 7（魔数）与维度 1（命名）
- 链式取值穿透（a.b.c.d）→ 计入维度 3（语法层）
- 注释该说"做什么、为什么"，不该复述"怎么做"；**行尾注释是代码有问题的信号**（代码没能自解释，才需要在行尾找补）→ 计入维度 9
- 命名两个极端都违规：无意义短名（读不出意图）与花哨长名（`CommandAllocationGroupSize` 式堆砌）——精准中长名才是抽象到位

**调用图度量**（扇入扇出四则）：

- **扇出过高**（它调用的别人 >5 个）：函数职责过多、复杂度中枢 → 提示拆分，计入维度 3/5
- **扇入过高**（调用它的入口很多）：本身是复用好的表现；但高扇入 + 有状态/有副作用 = 公共依赖热点，改动牵一片，必须提示"修改需重点回归" → 计入维度 4 作风险标注。高扇入 + 无状态 + 低扇出 = 理想底层工具，不扣分
- **扇入为零**（全库无调用者）：先分性质再裁——**通用封装层备而未用的公开能力（导出函数、声明、类、转发接口）不扣分**（见误报排除）；库内私有代码且全库无调用者的，才按死代码计入维度 9
- **调用深度**：调用链越长（a 调 b 调 c 调 d……）追溯越难，与"一跳可追踪"直接冲突 → 计入维度 4

**函数复杂度度量**：

- **圈复杂度**（独立执行路径数 ≈ 分支与循环个数 +1）：>10 拆分预警、>15 必拆 → 计入维度 5
- **认知复杂度**：嵌套越深惩罚越重（人脑的栈也有限）——嵌套 >3 层即违规，同层线性分支比深层嵌套轻
- **返回点**：函数开头的守卫早返回是正当的（见误报排除）；函数**中段**散落多个返回 = 控制流碎片化 → 计入维度 5
- **局部变量过多**（>7 个）：函数在同时盯太多东西，是"干了多件事"的直接证据 → 计入维度 5
- **副作用显式化**：函数改全局状态、改传入的参数对象、暗中做输入输出——藏着的副作用是数据流黑洞；偏好同输入同输出、无副作用的纯函数（业务编排函数例外，但副作用必须可见）→ 计入维度 4

**类级度量**（面向对象项目加查，函数式/无类项目跳过）：

- **类的方法复杂度总量**：一个类的方法个个复杂 = 类成了复杂度垃圾场 → 单一职责违规，计入维度 1
- **类的响应面**：一个类的方法合计四处调用多少外部方法——响应面过大 = 类的扇出过大，这个类知道得太多 → 计入维度 3
- **类间耦合数**：一个类直接操作多少个别的类 → 交给耦合标尺判级
- **方法群内聚缺失**：类的方法之间互不共享字段、各干各的 = 这个类是几个类硬拼出来的（内聚的量化形态）→ 拆，计入维度 1
- **继承深度与子类数**：继承链超过 2-3 层 = 脆弱的继承塔；子类众多而父类频繁修改 = 父类契约不稳 → 按里氏替换与合成复用原则检查

### AI 生成代码的三大指纹（识别整体性人机差异）

单看一个模块好像没问题、整体看不像人写的——AI 代码的病根在整体层。三大指纹：

1. **整体无思维连续性（封装不足的深层形态）**：人的代码有贯穿全库的设计意志——同一个问题到处是同一种解法、同类资源同一种管法、错误处理前后一致；AI 代码是逐次局部最优的堆叠——同一问题这个模块方案 A、那个模块方案 B，RAII 与手动释放混用、命名风格漂移、同类回调三种签名。**判据：抽十处同类代码横向对比，解法不一致超过三种 = 缺统一心智。**跨模块视角必须评：逐模块评全对、拼起来不连贯的，按维度 1/9 记"无贯穿主线"。
2. **幻觉防御（过度考虑不会发生的情况）**：为根本不会发生的输入写处理——为不可为空的值判空、为不可变数据防御性拷贝、为单一实现留多实现钩子、为想象中的边界写兜底。根源是不了解业务实际。**判据：每个防御分支问一句"触发条件在真实业务里存在吗？"答不出存在场景的，就是幻觉防御**——计入维度 8（校验放对位置），与"不可能状态不可表示"构成一对：真该防的用类型防，不该防的删掉。
3. **自我加戏（幻觉的需求）**：发明不存在的需求——没人提过的功能路径、臆造的协议细节、注释叙事与代码行为不符、配置项从未被任何业务读到、接口参数纯属想象。**判据：每个抽象、每个接口、每个配置项都应能回答"对应哪个真实需求"——答不出的就是加戏**，计入维度 9。

三者的共同本质：**用代码量购买想象中的兼容**——与"简单与灵活"一节正好反面呼应：真正的灵活靠抽象真实的变，AI 式膨胀靠防御想象的变。

- 组合根/程序入口处装配大量具体依赖 ≠ 依赖违规——入口就是干这个的
- 外部协议、线上格式、封闭枚举上的 switch ≠ 缺多态——对外封闭集合的显式分派是正当的
- 吸收外部变动、隔离不稳定性的薄包装层 ≠ 多余间接层——它有明确回报
- 跨越独立边界上下文的少量重复 ≠ 知识重复——边界处的独立拥有权可能更清晰
- 数据传输对象、持久化记录、接口载荷模型只有数据没有行为 ≠ 贫血模型——它们本来就只该有数据；贫血违规专指**业务**对象只剩 getter/setter
- 深而简单的模块（接口小、内部实现厚）≠ 问题——那正是封装到位的样子；要抓的是浅模块（接口比功能还复杂）
- 带守卫子句的线性长函数 ≠ 高认知负荷——读得顺就不算；要抓的是嵌套深、分支纠缠
- 测试中围绕非确定依赖的少量模拟、验证外部可观察事件的交互断言 ≠ 模拟滥用
- 高扇入的无状态纯工具函数 ≠ 违规——那正是理想底层工具的样子
- **未被引用的公开能力 ≠ 死代码 ≠ 违规**：通用封装层天生备而未用——导出的函数、声明的接口、定义的类、转发封装，当前业务没用到是正常状态，不扣分。两条例外照扣：①该能力**被引用且行为坑人**（返回 "TODO:" 的假实现进了活路径、恒真返回值、拷贝即丢数据）——问题在坑不在闲置；②非能力的**垃圾**（注释掉的尸体代码、`#if 0` 禁用的测试套件）——测试是质量保障不是封装能力，禁用即覆盖幻觉，照扣
- 函数开头的守卫早返回（非法输入提前出局）≠ 返回点过多——那是让主流程保持线性的正当手段

### 分数语义（锚点）

| 分数段 | 含义 |
|---|---|
| 90-100 | 教科书级，可作范本 |
| **80-89** | **规范**——结构清晰、分层方向正确、缺陷零星可修（大多数认真写的工程代码应落在这里） |
| 60-79 | 结构成立，存在系统性短板 |
| <60 | 结构性塌方 |

### 问题级别（P0-P3，报告标注体系）

| 级别 | 含义 | 对应范围 |
|---|---|---|
| P0 | 灾难级：架构整体不成立、根本无法安全使用 | 塌方级（对应封顶规则） |
| P1 | 严重：结构性缺陷，或同类问题成片已是默认写法 | 系统性 |
| P2 | 中等：多个模块/调用点存在同一模式 | 普遍 |
| P3 | 轻微：个别点状问题、改进建议 | 局部 |

**P0 理论上不该出现**：对结构清晰、方向正确的代码打出 P0，先回头查自己——多半是把行业惯例或点状缺陷上纲了。P0 只留给"这套代码不能要了"的情形。
**分数与判级必须一致**：总分落在塌方段（<60）或命中封顶条款时，把分数压到那里的主问题必须标 P0——塌方却只有 P1 是判级失真。

### 扣分幅度（每维通用）

- **P1/系统性**：该违规是项目默认写法，随处可见 → 扣 30-50
- **P2/普遍**：多个模块/多个调用点存在同一模式 → 扣 15-30
- **P3/局部**：1-2 处孤立违规 → 扣 5-12

### 结构锚定（分数由结构形态主导）

**分数评的是代码组织形态，不是 bug 数量。**

- **结构形态好 = 85 基线起**：模块边界清晰、封装收口到位（类型关门、出口唯一、暴露面最小）的代码，85 分起步；教科书级（可作范本）90+。内务与点状问题在此基础上按限幅小扣。
- **点状缺陷限幅**：修一处就好一处的点状问题（某处注入面、某行假返回、个别越界）合计拉低总分不超过 8 分——进 P1/P2 报告必须修，不决定水位。
- **重扣走结构主维度的重扣条款**（见"维度分两级"）：知识泄漏成默认写法、model 散布、业务渗层、跨服务散装共享、假分层、组装失控成群——每类 12-18 分，这是分数能压到 50 以下的唯一通道。
- 判据示例：封装模块化好 + 三处点状安全洞 = 85 分 + 三条 P1 报告；封装模块化好 + 内务一团糟 = 78-85 分；知识泄漏与散装共享成形态、无点状 bug = 45-60 分。

### 定档规则

- **范围定档，不定性质**：档位只看违规波及范围（一处/多处/默认写法），不因"性质严重"升档——某处 SQL 注入、某行契约撒谎，性质写进报告的定性文字，扣分按其实际范围（通常是局部档）。只有同类问题成片，才进普遍/系统档。
- **行业惯例从轻——但有严格边界**（越界照旧违规）：
  - 惰性单例获取函数：仅限**完全自包含、无业务状态**的基础设施（日志器、时钟）；持有业务状态、业务数据或输入输出的单例照旧违规，不作惯例。
  - 缺省值兜底：仅限**已有配置下发通道**的场景——配置体系到位、代码里留同值缺省才算兜底；没有配置体系的纯硬编码不算兜底，照旧违规。
  - 类型化重载族：仅当语言无更好收敛机制时从轻；有模板/宏/表驱动可收敛而不收敛的，照旧计入演进健康。
  - 构造函数启动工作线程、静态绑定三方库：从轻。
  - **单进程服务的组合根全局可达**（服务定位器：全局访问函数/宏取得入口持有的成员）：组合根形态完整——程序入口唯一创建、按依赖序启动、逆序停止、不跨进程边界对外——时从轻为 P2；越界条件：组合根自身是单例给自己提供全局点、底层模块反依赖高层服务、跨进程暴露——任一命中照旧按维度 10 计违规。
  - **进程内模块间共享上下文对象**（如会话对象携带多协议字段供进程内几个协作模块读写）：对外接口面（导出头/公共 API）已收口、字段不外泄进程、模块集合封闭在同一进程内时从轻为 P2（报告仍建议按协议拆值对象+语义函数）；越界条件：敏感字段（口令/密钥）裸露可读、上下文被无业务关系的模块搭车读写、跨进程/跨服务共享——照旧按"跨服务散装共享"重扣。
  - **从轻不等于不计**：从轻项仍列 P3/P2；同类从轻项成片（≥3 处）不得再降档；从轻只降档位，不改问题的存在性。
- **一次违规一次计罪**：同一问题只在主维度扣分，其他维度引用作佐证不重复扣（全局单例主罪在对象组装或数据流择一；硬编码主罪在策略分离；复制重复主罪在演进健康）。

### 一次违规一次计罪（细则）

- 全局单例/全局状态：主罪记维度 10（对象组装）或维度 4（数据流），二者择一；维度 6 只陈述"无注入点"的事实，不因此再扣。
- 硬编码可变语义：主罪记维度 7；维度 3 不为同一批魔数二次扣分（除非它同时构成知识泄漏，如错误码区间重叠）。
- 复制粘贴重复：主罪记维度 9；漂移成的功能 bug 不在评分维度扣（bug 是 bug，代码组织分只扣一次重复）。
- 报告其他维度时可以提同一证据，但**分数只在一处落**。

### 封顶规则（结构性缺陷直接限总分）

- 完全摆不出三层（数据、行为、工具混装一锅）→ 总分上限 **30**
- 共用 model 无集中定义点（散布各处）→ 总分上限 **40**
- 详细业务层无统一形态（散件类遍地）→ 总分上限 **50**

封顶规则与加权分取更低者。

## 分数即客观结论

- **评分器给出的分数就是最终分数**：不设"所有者终审分"覆盖，一份报告里只有一个分数。
- 分数只由代码实况决定：证据、维度判定、封顶与限幅规则说了算。评审者身份、项目状态、整改进度、团队约束、交付压力都不进入分数。
- 同一份代码换谁来评、什么时候评，分数应当落在同一位——分数不稳就是判定没落到证据上，回头补证据，不是调分。
- 已作出的判定不得为迎合任何外部预期而改分或改证据。分数与证据不符时，**改证据不改分数**。
- 报告的核心价值是**证据与 P0-P3 问题清单**：分数回答"现在是什么水平"，清单回答"要改什么"。

## 评分纪律

- 第一轮就给最终分数和立场，结论先行，不铺缓冲话术。
- **铁律：说人话**。评价文字短句直给：好就是好，烂就是烂，扣几分、为什么、怎么改，一句话说得清。禁用"但是/不过/总体而言尚可/一定程度上/需要注意的是/美中不足"之类转折与缓冲词——它们都是在给烂代码留面子。定量性陈述用陈述句，不用商量语气。
- **铁律：禁评分器黑话**。报告面向的是要改代码的人，不是评分器自己。内部术语一律翻译成日常工程语言再写：不说"单例全貌"，说"全局变量有多少个、都在哪"；不说"零收敛补丁堆"，说"补丁摞补丁、从没整理过"；不说"命中封顶条款"，说"数据结构散得到处都是，这条直接顶死总分上限"；不说"结构主维度决定水位/限幅/重扣"，说"大问题压总分，小毛病只进清单不压分"。判断依据引用本 skill 条款可以（写清违反哪条），但症状与定位必须用大白话。检验标准：把报告念给一个没读过本 skill 的工程师听，他不需要任何解释就能听懂哪里烂、烂成什么样。
- **铁律：评分器只读**。评分任务绝不修改、删除、移动被评项目的任何文件——哪怕评分者认为该删（发现问题写进报告，处置权在项目所有者）。删除/修改只在所有者明确下达指令时执行，且执行前在报告中声明影响范围。
- **诊断完成前不许给修复**：全部维度查完再谈怎么改——边查边给修复会把评审变成见招拆招的补丁对话。每条发现必须四段齐：症状（哪不对）→ 出处（违反本评分器哪条要素/原则/维度）→ 后果（伤害哪个质量属性）→ 修复方向（怎么改）。缺出处和后果的发现是噪音，不算数。
- **禁止拿被评项目自己的文档/惯例替现状辩护**（"项目规范说最小实现"= 循环论证）。按本评分器裁决。
- 无"部分成立/一定程度上"：每个违规点要么成立要么不成立，事实边界用陈述句说。
- 每个扣分必须落到证据：`文件:行号` + 一句定性。无证据不扣分，有证据必扣分。
- **铁律：评分的唯一输入是源代码与配置文件**。项目自带的说明文档、宣传材料一律不看、不参考、不引用——它们描述的是愿景不是实况，读了就会被引导。开工前不得打开项目里的说明文档；报告里的一切证据只允许来自源码与配置文件。**测试用例也不是输入——不读、不评、不当证据**（见维度 6）。若已知项目文档声称某架构，也**不许**拿它当检查线索——按代码自身的形态去找架构。
- **命名与文档的承诺不算数，兑现才算**：组件叫 config_manager / app_main / models，只说明意图不说明事实——逐项按代码实况验证承诺是否兑现（配置中心是否真有统一入口与快照、入口是否真是组装点、模型层是否只放数据）。命名兑现不了的，按"架构活在命名里"如实报告。
- 总分必须由维度分算出，不许跳过维度直接拍一个总分。

## 输出格式

**单项目完整报告**（固定六段，顺序不许换；好与不好都必须说透，各带证据）：

```
# 代码质量评分报告：<项目名>

## 总分：X / 100
一句话定位：段位归属 + 决定水位的形态一句话（分数是哪些主因拉下来的，点名到重扣条款或限幅规则）

## 好的方面
- <做对了什么>：<结论一句>（文件:行号）——结构形态好在哪必须说透

## 质量属性面板
| 属性 | 得分 | 主要拖累维度 |（8 属性；得分一律 0-100 分制，禁 10 分制/5 分制）

## 结构维度明细
| 维度 | 得分 | 关键证据 |（10 维度，得分一律换算为 0-100 分制——不写 x/15、x/20 这类权重分母；证据 = 文件:行号 + 一句定性）

封顶：命中/未命中（命中则写明哪条）

## 问题清单（按 P0→P1→P2→P3 排序，每条四段）
1. [P1] 文件:行号 — 症状：一句定性 — 出处：违反哪个要素/原则/维度 — 后果与修复方向：伤哪个属性、怎么改（各一句话）
```

**多项目汇报**（先总后分）：

```
# 汇总表
| 对象 | 分数 | 段位 |
每对象一节完整报告（单对象裁剪时质量属性面板可折叠为一句）
```

汇报纪律：
- **好的方面不许省略**：高分要说清好在哪（哪些形态不许在整改中改坏）；低分也必须列出做对的部分。只报问题的报告是半份报告。
- **压分主因必须可追溯**：读者要能从报告直接看出"分数是哪几条拉下来的"——每条主因点名到结构重扣条款、点状限幅或内务限幅，不许抛一堆问题让读者自己猜水位。

## 评前自检

- 想拿项目自身文档论证"现状合理" → 停，按本评分器裁
- 想打开项目里的说明文档找线索 → 停，铁律：唯一输入是源码与配置文件，文档一律不看
- 看到带架构意味的命名（manager/models/app_main） → 停，命名是承诺不是事实，逐项按代码验证兑现度
- 想写"部分成立" → 停，二选一
- 想写"但是/不过/总体尚可" → 停，删掉转折直接说结论
- 查调用方知识边界只看了链式取值 → 停，补查"调用方是否要懂内部规则"
- 想因为业务逻辑本身扣分 → 停，业务对错不在评分范围
- 硬编码想放行（"就一个值不会变"）→ 停，可变语义进配置
- 模块读起来"能跑"但说不清输入输出 → 停，按数据流或模块边界违规记录
- 看到新情况的接法是"再加一段代码"（新分支/新入口/新方法） → 停，问一句：这个变化能不能赶出控制流、赶进数据或类型里？能而不做，按"简单与灵活矛盾未处理"记录
- 对某个违规拿不准定性 → 停，回到四大要素问一句：这违反了封装/抽象/继承/多态的哪一条原理？违反了哪条就按哪条报
- 评类与接口的设计关系 → 过一遍七大原则表：职责单一吗？加情况要改旧代码吗？子类能无条件顶替吗？依赖的是抽象还是具体？接口胖不胖？该组合还是继承？调用方懂内部了吗？
- 评模块 → 两头判级：内部内聚到了哪级、对外耦合到了哪级；只要有一头落在违规级，就按对应维度记录
- 想报某问题前 → 先过一遍"误报排除"清单：命中任何一条"不应标记"就不报，不硬凑
- 理解某段代码需要借助调试器单步 → 停，这本身就是违规证据（代码没能自解释），按认知过载记录
- 看到特判 if（头/尾/某种特殊情况的专门分支） → 停，问一句：这个特例能不能被更好的数据结构整个消掉？
